# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/dm-is-slug.rb'

Hoe.new('dm-is-slug', DataMapper::Is::Slug::VERSION) do |p|
  p.rubyforge_name = 'dm-is-slug-gemfixx' # if different than lowercase project name
  p.developer('nik radford', 'nik@terminaldischarge.net')
end

